# Drop the brand assets here

The deck and the README expect two PNGs in this folder:

- `prisma-hero.png`     — the prism logo with figure inside (square)
- `prisma-closing.png`  — the prism with four refraction rays (16:9)

Once they're in place, both `../../README.md` and `../slides.md` pick
them up automatically. No code changes needed.
